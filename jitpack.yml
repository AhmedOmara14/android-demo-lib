jdk:
  - openjdk17

before_install:
  - ./scripts/prepareJitpackEnvironment.sh

install:
  - FILE="TestSdk-debug.aar"
  - GROUP="com.github.AhmedOmara14"
  - ARTIFACT_ID="android-demo-lib"
  - VERSION="1.0"
  - mvn install:install-file -Dfile=$FILE -DgroupId=$GROUP -DartifactId=$ARTIFACT_ID -Dversion=$VERSION -Dpackaging=aar
